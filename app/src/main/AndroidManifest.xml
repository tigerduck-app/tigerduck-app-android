<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name="org.ntust.app.tigerduck.TigerDuckApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TigerDuck"
        android:usesCleartextTraffic="false">

        <activity
            android:name="org.ntust.app.tigerduck.MainActivity"
            android:configChanges="locale|layoutDirection|screenSize|screenLayout|smallestScreenSize|orientation"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.TigerDuck"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="announcement"
                    android:scheme="tigerduck" />
            </intent-filter>
        </activity>


        <receiver
            android:name="org.ntust.app.tigerduck.notification.AssignmentNotificationReceiver"
            android:exported="false" />

        <receiver
            android:name="org.ntust.app.tigerduck.notification.ClassPreparingNotificationReceiver"
            android:exported="false" />

        <receiver
            android:name="org.ntust.app.tigerduck.liveactivity.LiveActivityBoundaryReceiver"
            android:exported="false" />

        <receiver
            android:name="org.ntust.app.tigerduck.notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.WidgetBoundaryReceiver"
            android:exported="false" />

        <receiver
            android:name="org.ntust.app.tigerduck.widget.receivers.WeekLightWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_week_light_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_week_light_info" />
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.receivers.WeekDarkWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_week_light_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_week_dark_info" />
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.receivers.TodayLightWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_today_light_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_light_info" />
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.receivers.TodayDarkWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_today_light_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_dark_info" />
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.receivers.NextClassLightWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_next_class_light_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_next_class_light_info" />
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.receivers.NextClassDarkWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_next_class_light_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_next_class_dark_info" />
        </receiver>

        <receiver
            android:name="org.ntust.app.tigerduck.widget.LibraryShortcutWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_library_shortcut_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_library_shortcut_info" />
        </receiver>

        <!-- App provides its own WorkManager configuration via Configuration.Provider
             so Hilt's worker factory is used. Disable the default initializer. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
